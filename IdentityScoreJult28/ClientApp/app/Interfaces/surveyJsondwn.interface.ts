export interface surveyJsondwn {
    surveyId?: number,
    survey_Name?: string,
    survey_Active?: boolean,
    calAddressScore?: boolean,
    calSocialScore?: boolean,
    calAgeScore?: boolean,
    calTwoFactorScore?: boolean,
    redirectingUrl?: string
    
}

